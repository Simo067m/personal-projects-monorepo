# Home Server
Repository for home web server running on a Raspberry Pi or other device.

### Updating the Application
To pull the latest code and safely rebuild the container run:

```bash
git pull
docker compose up -d --build
